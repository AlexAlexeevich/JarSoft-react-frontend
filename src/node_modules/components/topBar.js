import React from 'react'
import {Link, NavLink} from 'react-router-dom'

const TopBar = () => {
    return (
        <nav className='navbar navbar-light' style={{background: 'light', border: '2px solid black'}}>
            <div className='container-fluid'>
                <ul className='nav navbar-nav'>
                    <li className='nav-item'>
                        <NavLink to='/banners' className='nav-link' style={{color: 'black', fontWeight: 'normal'}}
                                 activeStyle={{color: 'deepskyblue', fontWeight: 'bold'}} exact>
                            Banners
                        </NavLink>
                    </li>
                    <li className='nav-item'>
                        <NavLink to='/categories' className='nav-link'
                                 className='nav-link' style={{color: 'black', fontWeight: 'normal'}}
                                 activeStyle={{color: 'deepskyblue', fontWeight: 'bold'}}>
                            Categories
                        </NavLink>
                    </li>
                </ul>
            </div>
        </nav>

    )
}

export default TopBar